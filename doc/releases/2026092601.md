# ZIMA-CAD-Parts 2026092601

Controls now use the native desktop style and palette. Tree selection, tabs,
checked buttons, menus and navigation respond to the system theme without
application-defined azure or green backgrounds. Unnamed parameter labels retain
their italic cue and use the normal system text colour. Rotated navigation labels
use the enabled or disabled button-text palette role. Generated directory tiles
use browser system colours in light and dark mode. Product artwork is unchanged.

The appearance change introduces no UI strings. Existing Czech, English, German,
French and Russian localization remains in place. See
[interaction colours](../interaction-colours.md) for scope and verification.

The full local GUI, CLI, updater and integration build passed. All 60
integration checks, 14 tool tests, four read-only CLI tests and four translation
catalog checks passed. A separate Windows native-style test passed light/dark
palette changes while preserving selection and checked-button state. Tool tests
required access to the system recycle bin outside the restricted sandbox.
Signed portable acceptance is recorded after package verification. Native Linux desktop rendering requires validation on Linux.
